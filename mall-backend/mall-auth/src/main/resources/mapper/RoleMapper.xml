<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyh.mallauth.mapper.RoleMapper">

    <resultMap id="BaseResultMap" type="com.cyh.mallauth.entity.Role">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="description" property="description" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT
            r.id, r.name, r.code, r.description, r.status, r.created_at, r.updated_at
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.status = 1
    </select>

    <!-- 根据角色编码查询角色 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT
            id, name, code, description, status, created_at, updated_at
        FROM roles
        WHERE code = #{code}
          AND status = 1
        LIMIT 1
    </select>

    </mapper>