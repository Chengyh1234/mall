<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyh.mallorder.mapper.CartItemMapper">

    <resultMap id="BaseResultMap" type="com.cyh.mallorder.entity.CartItem">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="sku_id" property="skuId"/>
        <result column="quantity" property="quantity"/>
        <result column="selected" property="selected"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="sku_specs" property="skuSpecs"/>
        <result column="price" property="price"/>
        <result column="expire_time" property="expireTime"/>
        <result column="notes" property="notes"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE user_id = #{userId} ORDER BY updated_at DESC
    </select>

    <select id="selectByUserIdAndSkuId" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE user_id = #{userId} AND sku_id = #{skuId} LIMIT 1
    </select>

    <select id="selectSelectedByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart_items WHERE user_id = #{userId} AND selected = 1 ORDER BY updated_at DESC
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM cart_items WHERE user_id = #{userId}
    </select>

    <delete id="deleteByUserId">
        DELETE FROM cart_items WHERE user_id = #{userId}
    </delete>

    <delete id="deleteSelectedByUserId">
        DELETE FROM cart_items WHERE user_id = #{userId} AND selected = 1
    </delete>
</mapper>