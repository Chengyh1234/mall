<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyh.mallportal.mapper.PermissionMapper">

    <resultMap id="BaseResultMap" type="com.cyh.mallportal.entity.Permission">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="type" property="type" />
        <result column="parent_id" property="parentId" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="icon" property="icon" />
        <result column="sort" property="sort" />
        <result column="status" property="status" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, name, code, type, parent_id, path, component, icon, sort, status, created_at, updated_at
    </sql>

    <select id="selectByRoleIds" resultMap="BaseResultMap">
        SELECT DISTINCT
        <include refid="Base_Column_List" />
        FROM permissions
        WHERE id IN (
            SELECT permission_id
            FROM role_permissions
            WHERE role_id IN
            <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                #{roleId}
            </foreach>
        )
        AND status = 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT
        <include refid="Base_Column_List" />
        FROM permissions
        WHERE id IN (
            SELECT rp.permission_id
            FROM role_permissions rp
            INNER JOIN user_roles ur ON rp.role_id = ur.role_id
            WHERE ur.user_id = #{userId}
        )
        AND status = 1
    </select>

</mapper>
