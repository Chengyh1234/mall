<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyh.mallproduct.mapper.SkuMapper">

    <!-- 根据SPU ID查询SKU列表 -->
    <select id="selectBySpuId" resultType="com.cyh.mallproduct.entity.Sku">
        SELECT id, spu_id, sku_code, price, market_price, cost_price, stock, warn_stock, image, weight, status, created_at, updated_at
        FROM sku
        WHERE spu_id = #{spuId}
        AND status = 1
        AND is_deleted = 0
        ORDER BY id ASC
    </select>

    <!-- 根据SKU编码查询 -->
    <select id="selectBySkuCode" resultType="com.cyh.mallproduct.entity.Sku">
        SELECT id, spu_id, sku_code, price, market_price, cost_price, stock, warn_stock, image, weight, status, created_at, updated_at
        FROM sku
        WHERE sku_code = #{skuCode}
        AND status = 1
        AND is_deleted = 0
    </select>

    <!-- 获取SPU的最低价格 -->
    <select id="selectMinPriceBySpuId" resultType="java.math.BigDecimal">
        SELECT MIN(price)
        FROM sku
        WHERE spu_id = #{spuId}
        AND status = 1
        AND is_deleted = 0
    </select>

    <!-- 获取SPU的库存总量 -->
    <select id="selectTotalStockBySpuId" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(stock), 0)
        FROM sku
        WHERE spu_id = #{spuId}
        AND status = 1
        AND is_deleted = 0
    </select>

</mapper>