<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyh.mallportal.mapper.CategoryMapper">

    <!-- 根据父分类ID查询子分类 -->
    <select id="selectByParentId" resultType="com.cyh.mallportal.entity.Category">
        SELECT id, name, parent_id, level, icon, sort, status, created_at, updated_at
        FROM categories
        WHERE parent_id = #{parentId}
        AND status = 1
        ORDER BY sort ASC, id ASC
    </select>

    <!-- 根据级别查询分类 -->
    <select id="selectByLevel" resultType="com.cyh.mallportal.entity.Category">
        SELECT id, name, parent_id, level, icon, sort, status, created_at, updated_at
        FROM categories
        WHERE level = #{level}
        AND status = 1
        ORDER BY sort ASC, id ASC
    </select>

    <!-- 查询所有子分类ID（递归） -->
    <select id="selectChildIds" resultType="java.lang.Long">
        SELECT id FROM categories WHERE parent_id = #{parentId} AND status = 1
    </select>

    <!-- 查询分类及其所有子分类 -->
    <select id="selectCategoryWithChildren" resultType="com.cyh.mallportal.entity.Category">
        SELECT id, name, parent_id, level, icon, sort, status, created_at, updated_at
        FROM categories
        WHERE id = #{categoryId} OR parent_id = #{categoryId}
        AND status = 1
        ORDER BY level ASC, sort ASC
    </select>

</mapper>
