server:
  port: 8888
  servlet:
    context-path: /api
spring:
  application:
    name: reggie_take_out
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ecommerce_platform?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB
  cache:
    redis:
      time-to-live: 180000
  data:
    redis:
      host: 192.168.31.140
      port: 6379
 # ==================== 邮件配置 ====================
  mail:
    default-encoding:
    host: smtp.qq.com
    port: 587
    username: 2265534937@qq.com
    password: xwypdgkcdnmdeade
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


file:
  upload:
    base-path: ./uploads
    images-path: ./uploads/images
    spu-path: ./uploads/images/spu
    sku-path: ./uploads/images/sku
    brands-path: ./uploads/images/brands
    stores-path: ./uploads/images/stores
    avatars-path: ./uploads/images/avatars
    banners-path: ./uploads/images/banners

mall:
  order:
    pay-expire-minutes: 30
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto #配置主键自增
      #逻辑删除配置
      logic-delete-value: 1 # 逻辑删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl